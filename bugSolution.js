function foo(a, b) {    if (a <= 0) { // Base case: a is less than or equal to 0        return b % 2 === 0; // returns true if b is even and false otherwise    } else if (b >= 10) { // another base case: b is greater or equal than 10        return a % 2 === 0; // return true if a is even and false otherwise     } else {        return foo(a - 1, b + 1);    }}console.log(foo(3, 0)); // Correct output will be printed without error